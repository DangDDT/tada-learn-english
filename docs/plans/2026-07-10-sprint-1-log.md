# Sprint 1 — Backend MVP Log

> Period: Jul 9–10, 2026 (planned Jul 14–28, completed early, backend only)
> Goal: Go REST API with Auth + CRUD Words + Docker + CI
> Frontend moved to Sprint 2 due to scope adjustment

## Summary

Sprint 1 was completed ahead of schedule with backend-only scope. All 8 issues fixed and merged. The backend is fully functional with auth endpoints and word CRUD operations. Docker setup and CI pipeline are operational. Frontend scaffold and pages deferred to Sprint 2.

## Completed Work

### Auth (backend)
- JWT-based authentication (access + refresh tokens)
- Register, Login, Refresh endpoints
- Forgot/Reset password flow (with password_reset_tokens table)
- Auth middleware with route protection

### Words CRUD (backend)
- Create word with IPA, CEFR, tags
- List with pagination, filtering (query, CEFR, SRS band, tag), sorting
- Get single word by ID
- Update with partial field support (dynamic SET clause)
- Soft delete
- Import from CSV with duplicate detection (ON CONFLICT DO NOTHING)

### Infrastructure
- Dockerfile (Go 1.25 alpine multi-stage)
- .dockerignore
- docker-compose.yml (backend + postgres)
- Bruno API test collection (11 tests covering auth, words, negative cases)
- GitHub Actions CI workflow (Bruno tests on push/PR to main)

### Bug Fixes (from code review)
- Fixed syntax errors in Go files (missing operators, spaces, declarations)
- Added uniqueness check on word update
- Made Import() transactional
- Fixed race condition in Create() with ON CONFLICT
- Fixed password reset stubs (implemented real logic)
- Added pagination, filtering, sorting to List()
- Fixed SQL WHERE clause operators

## Known Gaps
- No Go unit tests yet
- No golangci-lint config
- Missing .env.example
- Frontend not started

## Decisions
- Using pgx directly (no ORM)
- Soft delete with deleted_at IS NULL pattern
- Dynamic field update via pointer fields + COALESCE
